# AMOURANTHRTX field whitelist — extend consumer-safe process names.
# Peripherals and network stacks should never trigger behavior alerts.

NEXUS_DEVICE_WHITELIST_COMM+=(
  fwupd
  power-profiles-daemon
  gvfsd
  tracker-miner-fs
  evolution-source-registry
)